## Codebase Patterns
- RepoRef now uses `repo_id` (field) instead of `repo_id()` (method)
- RepoRef now uses `clone_url()` method or `git_url` field instead of `git_url()` method
- Use `paths::repo_cache_path(&repo_ref)` to get full cache path for a repository
- For conflict checking with remote repos, extract base repo ID with `repo_id.split('/').take(3).collect::<Vec<_>>().join("/")`
- Local paths use `local:` prefix in repo_id, remote use `host/path` format
- Unit tests must use placeholder data: testowner, testteam, testrepo, test-skills, example.com, etc. (no real usernames)
- Git module: error messages must include repository path/URL and git's stderr (NFR-002)
- Agent struct mirrors Skill struct: same fields (repository, *_path, enabled, enabled_at, disabled_at) and helper methods
- Integration struct has skills_dir and agents_dir as Option<String>; use `if let Some(ref dir) = integration.skills_dir` pattern
- Use `paths::get_builtin_agents_dir(name)` for agent directories (returns None for codex)
- BuiltinIntegration struct in paths.rs has name, skills_dir (Option), agents_dir (Option)
- Use `scan_for_agents()` alongside `scan_for_skills()` to discover both types during repo add/upgrade
- When reconciling agents in upgrade, use `reconcile_agents()` parallel to `reconcile_skills()`
- CLI subcommand groups follow pattern: SubagentAction enum mirrors SkillAction, handlers go in commands/subagent.rs
- Agent symlinks are FILE symlinks: `{agents_dir}/{name}.md` -> `cached/path/AGENT.md` (unlike skills which are directory symlinks)
- Use `paths::expand_tilde()` when working with user-provided directory paths (like agents_dir from config)

---

## 2026-01-18 - US-012
- Implemented sm list to show both skills and agents with type prefixes
- Files changed:
  - `src/commands/skill.rs` - Added `list_combined()` function for unified listing
  - `src/main.rs` - Updated `Command::List` to call `list_combined()` instead of `list()`
- **Changes made:**
  - New `list_combined()` function collects both skills and agents
  - Items sorted alphabetically by name
  - Each item prefixed with `[skill]` (blue) or `[agent]` (magenta)
  - Table format: TYPE, NAME, STATUS, REPOSITORY columns
  - Existing `list()` function renamed to `list_skills_only()` for `sm skills list`
  - Default shows enabled items only; --all shows all; --status filters
  - 75 tests pass
- **Learnings for future iterations:**
  - Combined list commands collect items from multiple config sources (skills + agents)
  - ItemType enum with derived Ord allows stable sorting by type when names match
  - Use different console colors to visually distinguish item types in output

---

## 2026-01-18 - US-011
- Implemented sm subagents list command
- Files changed:
  - `src/commands/subagent.rs` - Implemented list() function mirroring skill.rs pattern
- **Changes made:**
  - `list()` function reads config, filters agents by enabled status or --status flag
  - Default behavior shows only enabled agents
  - `--all` flag shows all agents (enabled and disabled)
  - `--status enabled|disabled` filters by specific status
  - `--name-only` outputs just agent names (one per line)
  - Table format matches skills list: SUBAGENT, STATUS, REPOSITORY columns
  - Shows helpful message when no enabled agents found pointing to --all flag
  - 75 total tests pass
- **Learnings for future iterations:**
  - Subagent list follows the same pattern as skill list - mirror the structure
  - Agent struct has same `enabled` field and filter logic as Skill struct
  - Output format consistency matters: both use 80-char separator line, same column widths

---

## 2026-01-18 - US-009
- Implemented sm subagents enable command
- Files changed:
  - `src/commands/subagent.rs` - Implemented enable() and disable() functions with helper methods
- **Changes made:**
  - `enable()` function looks up agent by name in config, enables it, creates symlinks
  - `disable()` function removes symlinks and marks agent as disabled in config
  - `create_agent_symlink()` creates FILE symlink (not directory) pointing to AGENT.md
  - `create_agent_symlinks_for_all_integrations()` creates symlinks in all integrations with agents_dir
  - `remove_agent_symlinks_from_all_integrations()` removes symlinks when disabling
  - Agent symlinks use `.md` extension: `{agents_dir}/{name}.md`
  - 3 unit tests for symlink creation/replacement/failure
  - 75 total tests pass
- **Learnings for future iterations:**
  - Agent symlinks are FILE symlinks to the AGENT.md file, not directory symlinks (unlike skills)
  - Agent symlink naming convention: `{agents_dir}/{agent-name}.md`
  - Must use `paths::expand_tilde()` to handle `~/` in user-configured directory paths
  - Skip integrations that don't have agents_dir (like codex which only supports skills)
  - Agents must be registered via `sm repo add` first - no auto-discovery during enable

---

## 2026-01-18 - US-008
- Added sm subagents CLI command group with subcommands
- Files changed:
  - `src/cli.rs` - Added SubagentAction enum with enable/disable/list subcommands, added Subagents command variant with aliases
  - `src/commands/mod.rs` - Added subagent module
  - `src/commands/subagent.rs` - Created with placeholder functions for enable/disable/list
  - `src/main.rs` - Added SubagentAction import and handler routing
- **Changes made:**
  - SubagentAction enum mirrors SkillAction pattern
  - Aliases: subagents, subagent, sb (all work)
  - List subcommand has --all, --status, --name-only flags (matching skills list)
  - Placeholder implementations print "not yet implemented" messages
  - 72 total tests pass (no new tests needed for CLI structure)
- **Learnings for future iterations:**
  - CLI command groups follow consistent pattern: enum for actions, command handler module
  - Aliases defined via `#[command(visible_aliases = ["alias1", "alias2"])]`
  - Placeholder functions can use underscore prefix for unused params (_param) to avoid warnings

---

## 2026-01-18 - US-007
- Added AGENT.md discovery to repo scanning
- Files changed:
  - `src/commands/repo.rs` - Added scan_for_agents(), reconcile_agents(), updated add() and upgrade_with_lock()
- **Changes made:**
  - New `scan_for_agents()` function finds directories containing AGENT.md (parallel to scan_for_skills)
  - New `reconcile_agents()` function handles agent changes during repo upgrade
  - `add()` now scans for both skills and agents, registers agents in config as disabled
  - `upgrade_with_lock()` now reconciles both skills and agents, shows [skill]/[agent] prefixes in output
  - Output message format updated to show "X skills, Y agents" or "X skills" or "Y agents" etc.
  - 10 new unit tests for agent/skill discovery functions
  - 72 total tests pass
- **Learnings for future iterations:**
  - scan_for_agents() and scan_for_skills() are identical except for the MD file name (AGENT.md vs SKILL.md)
  - reconcile_agents() mirrors reconcile_skills() - use the same pattern for agent symlink paths (with .md suffix)
  - A directory with both SKILL.md and AGENT.md is treated as both a skill and an agent (per edge case test)
  - Agent symlinks go to `{agents_dir}/{name}.md` (file symlink), skills go to `{skills_dir}/{name}/` (directory symlink)

---

## 2026-01-18 - US-006
- Updated Integration model with agents_dir support
- Files changed:
  - `src/config/state.rs` - Integration struct now has skills_dir: Option<String> and agents_dir: Option<String>
  - `src/paths.rs` - Added get_builtin_agents_dir(), BuiltinIntegration struct
  - `src/commands/integration.rs` - Updated add/remove/list/configure for both directories
  - `src/commands/repo.rs` - Updated to handle Option<String> skills_dir
  - `src/commands/skill.rs` - Updated to handle Option<String> skills_dir
  - `src/commands/purge.rs` - Added agent symlink cleanup
- **Changes made:**
  - Integration.skills_dir and agents_dir are now Option<String>
  - Built-in integrations: claude-code (both), opencode (both), gemini-cli (both), codex (skills only)
  - Integration list command shows both directories in columns
  - Purge command cleans agent symlinks too
  - 5 new unit tests for Integration with both/either/neither directory
  - 62 total tests pass
- **Learnings for future iterations:**
  - Use `if let Some(ref dir_str) = integration.skills_dir` pattern to handle Option<String>
  - Backward compatibility: serde deserializes old `skills_dir = "path"` into Some("path") automatically
  - Use `#[serde(default, skip_serializing_if = "Option::is_none")]` for optional fields

---

## 2026-01-18 - US-005
- Added Agent data model to config/state.rs
- Files changed:
  - `src/config/state.rs` - Added Agent struct, agents HashMap to Config, helper methods, unit tests
- **Changes made:**
  - Agent struct: repository, agent_path, enabled, enabled_at, disabled_at (mirrors Skill struct)
  - Config.agents: HashMap<String, Agent>
  - Helper methods: add_agent, enable_agent, disable_agent, remove_agent, has_agent
  - Query methods: agents_for_repo, enabled_agents
  - 8 new unit tests for Agent operations and TOML serialization
  - 57 total tests pass
- **Learnings for future iterations:**
  - Agent struct mirrors Skill struct pattern - use same fields and method patterns
  - Config helper methods follow consistent naming: add_*, enable_*, disable_*, remove_*, has_*, *_for_repo
  - Backward compatibility: use `#[serde(default)]` for new HashMap fields
  - Unit tests use placeholder domains: example.com/testowner/testrepo
---

## 2026-01-18 - US-004
- Verified `sm repo add` command supports universal git URLs
- Files verified:
  - `src/commands/repo.rs` - `add()` function uses RepoRef.parse() and paths::repo_cache_path()
- **Implementation status:** Already complete via US-001/US-002/US-003
  - RepoRef.parse() handles HTTPS, SSH, and local filesystem paths
  - paths::repo_cache_path() generates unique cache paths for any URL format
  - git::clone_repo() works with any git source (HTTPS, SSH, local)
  - All 49 tests pass
- **Learnings for future iterations:**
  - User-facing features may already be complete after implementing their underlying modules
  - Verify end-to-end flow even when components are implemented separately
---

## 2026-01-18 - US-003
- Updated git module to handle universal sources (HTTPS, SSH, local paths)
- Files changed:
  - `src/git/mod.rs` - Improved error messages, added documentation, added integration tests
- **Learnings for future iterations:**
  - Git clone/pull works natively with local paths - no special handling needed
  - Error messages should include: the URL/path attempted, and the full git stderr
  - Use `tempfile::TempDir` for creating temporary git repos in tests
  - Test helper `create_test_repo()` creates a git repo with one commit for testing
  - When cloning from local path, origin points to source, so pull_to_latest works
  - Now 49 tests pass (7 new git module tests added)
---

## 2026-01-18 - US-002
- Updated cache directory structure for universal URLs
- Files changed:
  - `src/paths.rs` - Added `repo_cache_path()` helper function and comprehensive tests
  - `src/commands/repo.rs` - Refactored to use new `paths::repo_cache_path()` helper
- **Learnings for future iterations:**
  - Use `paths::repo_cache_path(&repo_ref)` to get full cache path instead of manually joining paths
  - Cache structure: remote repos use `{host}/{owner}/{repo}`, local use `local/{dirname}-{hash}`
  - GitHub paths remain backward compatible: `github.com/owner/repo`
  - Special characters in paths (like spaces) are handled by hashing for local paths
  - Now 42 tests pass (10 new path-related tests added)
---

## 2026-01-18 - US-001
- Implemented universal git URL parsing in skill_ref.rs
- Files changed:
  - `src/skill_ref.rs` - Major refactor of RepoRef to support HTTPS, SSH, and local paths
  - `src/commands/repo.rs` - Updated to use new RepoRef API
  - `src/commands/skill.rs` - Updated to use new RepoRef API
  - `src/commands/integration.rs` - Updated to use new RepoRef API
  - `CHANGELOG.md` - Added documentation of the new feature
- **Learnings for future iterations:**
  - The RepoRef struct changed significantly - no more `owner` and `repo` fields, instead use `repo_id`, `git_url`, `source_type`
  - Cache paths are now generated via `cache_path()` method which handles different source types
  - Conflict checking logic is only needed for remote repos (local paths are always unique)
  - All 32 tests pass including new tests for GitLab, SSH, and local path parsing
---

## 2026-01-18 - US-000
- Replaced live data in unit tests with mock/placeholder data
- Files changed:
  - `src/skill_ref.rs` - Updated all 32 tests to use placeholder data
- **Changes made:**
  - Replaced `jroslaniec` → `testowner`
  - Replaced `j.roslaniec` → `test.user`
  - Replaced `anthropics` → `testorg`
  - Replaced `myteam` → `testteam`
  - Replaced `agent-skills` → `test-skills`
  - Replaced `agentfiles` → `testfiles`
  - Replaced `git.company.com` → `git.example.com`
  - All group/subgroup names updated to generic placeholders
- **Learnings for future iterations:**
  - Unit tests should always use placeholder data (testowner, testrepo, example.com, etc.)
  - Real usernames/URLs are only for manual testing sections in PRD
  - NFR-006 in prd.md requires mock data for unit tests
---
